# deploy.sh is the authoritative entry point for this sample — it handles
# scripts, the AgentTemplate, and the skill bundle directly via `create
# scripts`, `deploy agent`, and `create skill`. Don't re-upload any of that
# through `deploy configs`.
README.md
env.example
.env
deploy.sh
agent.yaml
scripts/
skills/
examples/

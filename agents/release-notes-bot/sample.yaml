schema_version: 2
version: v0.2.0
name: "Release Notes Bot"
tagline: "Drafts the weekly changelog by scanning merged PRs on a schedule."
min_cli_version: "0.28.0"

steps:
  - type: upload_scripts
    source_dir: scripts
  - type: deploy_agent
    template_file: agent.yaml

# The generic FDE Agent has no required env vars out of the box.
#
# Add customer-specific API keys, tokens, or service credentials only
# after you add tools or scripts that need them. Store secrets through
# your approved environment variable or secret-management flow; do not
# commit real values here.

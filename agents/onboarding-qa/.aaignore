# deploy.sh is the authoritative entry point for this sample — it uploads
# every file below directly via the CLI (deploy agent), and
# upload-knowledge.sh uploads the knowledge files as ingestion sources.
# Listing them here prevents `deploy configs` from re-uploading and
# clobbering what those scripts already installed.
README.md
env.example
.env
deploy.sh
upload-knowledge.sh
agent.yaml
knowledge/
examples/

"""
Helpers for scripts/sample_tool.py — split into modules so each piece
(validation, render, scaffold, pack, generate) can be reasoned about
on its own. Nothing imports from this package outside scripts/.
"""

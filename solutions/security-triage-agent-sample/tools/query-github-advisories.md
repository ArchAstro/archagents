# Query GitHub Advisory DB

Query GitHub Advisory Database for vulnerabilities affecting a package

Standalone tool. Attach to any existing agent to expand its capabilities.

## Setup

- **`GITHUB_TOKEN`** — GitHub token used to read the Advisory Database. A fine-grained
- **`DEFAULT_ECOSYSTEM`** — Default package ecosystem (e.g. `hex`, `npm`, `pip`). Used when
- **Install `integration/github`** — Install the ArchAstro GitHub App so the script's Advisory DB

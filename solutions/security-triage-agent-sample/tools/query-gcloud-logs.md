# Query Cloud Logging

Query Google Cloud Logging for security-related events

Standalone tool. Attach to any existing agent to expand its capabilities.

## Setup

- **`GCLOUD_PROJECT_ID`** — GCP project id with Cloud Logging enabled.
- **`GCLOUD_SA_KEY`** — Service account JSON with `logging.viewer` (read-only). Paste

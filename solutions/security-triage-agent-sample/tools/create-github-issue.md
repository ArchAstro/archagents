# Create GitHub Issue

File a GitHub issue for a security finding that needs human attention

Standalone tool. Attach to any existing agent to expand its capabilities.

## Setup

- **`GITHUB_TOKEN`** — GitHub token with `Issues: read/write` on the target repos.
- **Install `integration/github`** — Install the ArchAstro GitHub App on the target repos.

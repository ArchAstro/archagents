# Cross-Team Bug Relay — environment template.
#
# The solution itself requires no secrets: both agents ship with builtin
# tools only. Add entries here as you connect real tracker / product
# tooling to the installed agents (e.g. an MCP server token for your
# issue tracker on the reporter, or product API credentials on the
# resolver).

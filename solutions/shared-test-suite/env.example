# GitHub repository secrets (do not commit the real values)
ARCHAGENTS_TEST_WEBHOOK_URL=https://platform.archastro.ai/webhooks/<app-id>/test-results
ARCHAGENTS_TEST_WEBHOOK_SECRET=<generated-HMAC-secret>
ARCHAGENTS_TEST_TEAM_ID=tem_<internal-team-id>

# Optional GitHub Actions variables
SUITE_ID=stripe-paper
TEST_MODE=suite
# TEST_KEYS=invoice_void,invoice_create

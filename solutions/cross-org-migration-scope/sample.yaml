schema_version: 2
version: v0.1.0
name: Cross-Org Migration Scope
tagline: Cross-org migration scoping — a vendor Orchestrator dispatches breaking-change search requests to a partner org's Researcher as durable workflow runs.
min_cli_version: "0.28.0"
steps:
  - type: upload_configs
    source_dir: schemas
  - type: upload_configs
    source_dir: workflows
  - type: deploy_solution
    solution_file: solution.yaml

# Resolve Review Threads

Resolve your own unresolved review comment threads on a PR. Use only on synchronize events after verifying fixes.

- Function: `resolve_review_threads`
- Handler script: `resolve-review-threads`

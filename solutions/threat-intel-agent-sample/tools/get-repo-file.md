# Get Repo File

Read a file from a GitHub repository (e.g. mix.exs, package.json, requirements.txt) to check if a vulnerable package is in use.

- Function: `get_repo_file`
- Handler script: `ti-get-repo-file`

# The ArchAstro Onboarding agent has no required env vars.
#
# Runtime creation through createAgentAction should use the default
# template ref (`default-agent`), provision a backing thread, add the
# onboarding agent as a member, and merge thread_id into metadata.
#
# The bundled docs-fetch script hits public docs.archagents.com and does
# not require credentials.

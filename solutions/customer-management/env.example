# The Customer Success Agent has no required env vars out of the box.
#
# It uses builtin tools only and ships no credentials. Store any secrets your
# own customizations need through ArchAstro environment variables — never in
# agent prompts, identities, or configs.

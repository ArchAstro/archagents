# Weekly Changelog — March 31 to April 6, 2026

**12 PRs merged** across 6 contributors this week. Highlights below.

---

> **Highlight:** Webhook retry with exponential backoff (#389) lands after two weeks of soak testing. This replaces the fixed-interval retry that caused cascade failures during the March 19 incident.

---

## Features

- **Webhook retry with exponential backoff** — Retries failed webhook deliveries up to 5 times with jittered backoff. Configurable per-endpoint. (#389, @mchen)
- **Team-scoped API keys** — API keys can now be scoped to a team instead of a single user. Keys inherit the team's permission set. (#394, @priya)
- **CSV export for audit log** — Adds a "Download CSV" button to the audit log page. Filters are applied before export. (#401, @dlewis)

## Fixes

- **Fix timezone offset in scheduled reports** — Reports scheduled in non-UTC timezones were firing at the wrong hour. (#397, @jpark)
- **Handle null avatar URL in member list** — Null avatars caused a crash in the team settings page. Falls back to initials. (#399, @mchen)

## Infrastructure

- **Upgrade PostgreSQL from 15.4 to 16.2** — Performed during the Sunday maintenance window. No schema changes required. (#392, @ops-bot)
- **Add OpenTelemetry spans to payment flow** — Traces now cover the full checkout → charge → receipt path. (#396, @agarcia)

## Documentation

- **Add runbook for webhook delivery debugging** — Step-by-step guide for tracing a failed delivery from alert to root cause. (#400, @dlewis)

---

### Contributors

@mchen (3) · @dlewis (2) · @priya (1) · @jpark (1) · @agarcia (1) · @ops-bot (1)

*Generated by release-notes-bot from merged PRs on `main`. Edit labels to recategorize.*
